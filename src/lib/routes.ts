import { Home, LineChart } from 'lucide-react';

const routes = [
  {
    name: 'Početna',
    icon: Home,
    route: '/',
  },
  {
    name: 'Izveštaji',
    icon: LineChart,
    route: '/reports',
  },
];

export default routes;
